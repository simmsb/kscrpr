# UUh yeah this does something

```sh
kscrpr 0.1.0
Download stuff from naughty website

USAGE:
    kscrpr [OPTIONS] <SUBCOMMAND>

OPTIONS:
        --base-dir <BASE_DIR>    The directory to store data in [env: KSCRPR_BASE_DIR=] [default:
                                 /home/ben/Documents/kscrpr/]
        --base-url <BASE_URL>    The base url to fetch from [env: KSCRPR_BASE_URL=]
    -h, --help                   Print help information
    -V, --version                Print version information

SUBCOMMANDS:
    dir        Print a data dir
    fetch      Fetch archives from the site
    get        Get local archives matching criteria
    help       Print this message or the help of the given subcommand(s)
    reindex    Rebuild symlinks and the tantivy searcher
```
